Read CLAUDE.md first, then POST_RUN_DEBRIEF_SPEC.md and VISUAL_ACCEPTANCE_CHECKLIST.md. Treat the three PNG files in references/ as authoritative visual targets, not inspiration.

Before editing code, inspect the current Inte-Run debrief, project architecture, data flow, design tokens and shared heart-rate-zone implementation. Return a concise preflight report listing:
1. the files/components you will reuse or change;
2. the canonical existing Zone 1-5 colour tokens/component in light and dark mode;
3. existing import, save, sync, Strava, plan-matching, notes, shoes and privacy behaviour that must be preserved;
4. any genuine conflict or blocker.

Do not write code until the preflight is complete. After approval, implement the supplied design exactly, produce screenshots of the three target states, compare them by overlay/image diff, correct material differences and complete the acceptance checklist.

The only approved visual substitution is to use Inte-Run's existing Zone 1-5 heart-rate colours instead of the teal placeholder zone bars in the analysis reference. Do not invent new zone colours.
